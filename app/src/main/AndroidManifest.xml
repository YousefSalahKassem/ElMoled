<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elmoledmol.www">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Elmoledmol"
        android:usesCleartextTraffic="true">
        <activity android:name=".showmyaddresses"></activity>
        <activity android:name=".showimage" />
        <activity android:name=".sellalltop" />
        <activity android:name=".processsuccess" />
        <activity android:name=".ordercheckout" />
        <activity android:name=".myCartActivity" />
        <activity android:name=".seeAllActivity" />
        <activity android:name=".productInformation" />
        <activity android:name=".MainActivity" />
        <activity android:name=".seeAllSearch" />
        <activity android:name=".SplashLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>